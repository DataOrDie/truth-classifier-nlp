Differences: team-writeup.txt → team-writeup2.txt
Section 1 — Introducción (line 3)
Added one sentence announcing the final result:

"El mejor resultado del equipo alcanzó un F1 Macro de 0,69638 en el leaderboard público de Kaggle, para lograrlo utilizamos una estrategia híbrida de distillation desde un ensemble de Mistral-7B y DeBERTa-v3-large."

Section 3 — Dataset description (lines 11–15)
Rewritten to be more precise and data-driven:

Added test set size: 3.836 muestras de test (was unspecified)
Added exact null percentages: speaker_job (27,7 %), state_info (~22 %)
Added exact cardinality numbers: 2.634 valores únicos in speaker, 1.018 in speaker_job, 84,6 % of jobs appear < 5 times
Removed the softer prose ("También se detectaron…", "Otro aspecto relevante…")
Section 3 — Tareas Comunes (lines 19–21)
Expanded tools list to include: Kaggle Notebooks (GPU T4), PEFT (LoRA), bitsandbytes (cuantización 4-bit) alongside existing libraries
Extended hyperparameter list to include: threshold de decisión and warmup
Removed "Finalmente," transitional opener
Section 4 — Análisis (lines 24–33)
Removed the categorical-variable cardinalidad paragraph (absorbed into Section 3)
Replaced the vague Transformer preprocessing description with a specific enriched-text format schema:

Speaker: <speaker> | Party: <party> | Job: <job> | State: <state> | Topic: <subject> [SEP] Claim: <statement>
Plus a concrete Donald Trump example showing the format in action. The model is now named model_text.
Also explicitly adds ELECTRA and Mistral-7B to the transformer list here.
Section 5.3 — Transformers (lines 50–53)
Completely rewritten. Replaces the vague description of late-fusion stacking with detailed descriptions of the two best models:

DeBERTa-v3-large (435M params): class weights (1.85, 1.0), label smoothing 0.05, cosine LR with warmup, batch_size=8 + gradient accumulation to fit GPU T4
Mistral-7B + LoRA: LoRA justification (~10-50 MB trainable vs 14 GB full model), described as the strongest individual model
Section 5.4 — Entirely new section: Estrategias de ensamble
Three new subsections not present in the original:

Subsection	Description
5.4.1 Voting con confianza Mistral × DeBERTa-large	Override Mistral predictions where DeBERTa disagrees with ≥0.70/≤0.30 confidence; 175/3836 flipped (4.6%)
5.4.2 Speaker-aware voting	Adaptive thresholds per speaker: relaxed (0.50/0.50) for high-precision speakers (OOF >80%), strict (0.85/0.15) for unreliable ones
5.4.3 Distillation desde el voting ensemble	Voting predictions used as pseudo-labels for test set; new DeBERTa-v3-large trained on train + 3,836 pseudo-labels at weight 0.7 — the technique that broke the team ceiling
Section 6 — Evaluación y resultados (lines 65–92)
Replaced the HACER TABLA placeholder with an actual results table (10 models, from baseline 0.623 up to distillation 0.696)
Added two entirely new analytical blocks:
"Que funcionó" — explains why metadata concatenation + Mistral's architectural diversity + distillation were key
"Que no funcionó" — details failed experiments: self-distillation (0.98 correlation with original), adding RoBERTa/ELECTRA to voting (hurt ensemble), Qwen2.5-7B timeout on Kaggle, over-engineered classical stacks overfitting to CV folds
Section 7 — Conclusiones (lines 96–99)
Updated the "best model" sentence: replaced "DeBERTa-v3-base + stacking / late fusion" with "distillation desde un voting ensemble (Mistral-7B × DeBERTa-large)" as the key innovation
TL;DR: The new team member added all the LLM-related work — Mistral-7B + LoRA, the three ensemble strategies (voting, speaker-aware, distillation), the actual results table with real scores, and detailed post-mortem analysis of what worked and what didn't. The distillation technique (section 5.4.3) is the central contribution, lifting the team score from 0.689 → 0.696.
